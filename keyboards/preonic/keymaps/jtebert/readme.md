# Julia Ebert's Preonic Layout

Build it:

```shell
make preonic/rev3:jtebert
```

I had to install the following dependencies to get it to compile:

```shell
sudo apt install dfu-util gcc-arm-none-eabi
```